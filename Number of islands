class Solution {
    private void dfs(char grid[][],int R,int C,int row,int col){
        grid[row][col]='0';
        int dif[][]={{0,-1},{0,1},{-1,0},{1,0}};
        for(int i=0;i<4;i++){
            int ar=row+dif[i][0];
            int ac=col+dif[i][1];
            if(ar>=0 && ar<R && ac>=0 && ac<C && grid[ar][ac]=='1'){
                dfs(grid,R,C,ar,ac);
            }
        }
    }
    public int numIslands(char[][] grid) {
        int R =grid.length;
        int C=grid[0].length;
        int is=0;
        for(int i=0;i<R;i++){
            for(int j=0;j<C;j++){
                if(grid[i][j]=='1'){
                    is++;
                    dfs(grid,R,C,i,j);
                }
            }
        }
        return is;
    }
}
